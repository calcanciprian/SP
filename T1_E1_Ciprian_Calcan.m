a=0:0.1:2 %formez vectorul a de la 0 la 2 cu un pas de 0,1
c=a(:) %pun vectorul a sub forma de vecor coloana pt a avea sens la inmultirea elemet cu element
b=ones(size(c)) %formez vectorul b prin marimea vectorului a
rsp1=a*b % a)

rsp2=b*a % b)

rsp3=c.*b % c), se obtine ca rezultat vectorul a
